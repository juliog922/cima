---
name: Bug report
about: Something behaved incorrectly
labels: bug
---

**Startup banner** (the first `config:`/`serve:` log lines — they carry
version, GPU, and resolved configuration):

```
paste here
```

**What happened / what was expected**

**Reproduction** — exact commands, model name including quant tag:

```
cima ...
```

**Logs with `CIMA_LOG=debug`** around the failure (or `cima logs`):

```
paste here
```
