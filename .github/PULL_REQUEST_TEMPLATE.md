## What

## Why

## Test evidence
- [ ] `cargo test` passes
- [ ] `cima selftest` (required for kernels.cu / cuda.rs / quant.rs changes)
- [ ] `scripts/test.sh` (required for api.rs changes)
- [ ] before/after `scripts/benchs.sh` numbers attached (performance claims)
- [ ] registry.toml + vet/preflight report attached (new models)
